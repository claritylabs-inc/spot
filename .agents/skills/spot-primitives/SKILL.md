---
name: spot-primitives
description: "Use before adding or changing Spot shared primitives: UI components, feature flags, model routes, extraction helpers, workflows, agent tools, auth helpers, notification channels, or cross-cutting backend libraries."
---

# Spot Primitives

Use this skill before adding a shared abstraction or when a task risks duplicating an existing owner. The canonical catalog is in `AGENTS.md` under `Primitive Catalog And Reuse Rules`; keep this skill and that section aligned.

## Rule

All tables keep action buttons, action menus, edit controls, and dropzones out
of rows. Rows display data and open a keyboard-accessible sidebar. Editing and
uploads belong in the sidebar; record actions belong in its footer. Avoid
redundant sidebar section titles. Upload/extraction progress uses toasts.
Broker/client selectors use `OrgBrandIcon` through `SearchableSelect`'s optional
option icon, including the selected value.
Use `SearchableSelect.ariaLabel` when a tab already supplies the visible context.
`FileDownloadButton.iconOnly` serves compact PDF toolbars while retaining real
downloads. `SettingsDrawer` owns focus entry/restoration and Escape via its
nonmodal Base UI dialog, portaled into the app-shell sidebar; callers retain
their save-before-close guard. Sibling previews remain accessible, and footer
measurements keep toasts above actions.
Wiki, notification and compliance editors reuse autosave and preserve failed
drafts on close. Notification resets remove only the current user's overrides.
Connection rows navigate to details/footer actions; revocation invalidates
pending invitation access and OTPs at the tenant-admin boundary.
Request attachments use sidebar staging and shared footer preview/download.
Upload surfaces retain staged files until explicit success; `FileDropZone`
ignores drops while disabled. Archived thread rows use existing conversation
actions instead of a duplicate restore action in the list.

Search before creating:

```bash
rg "<concept|route|component|table|helper>" AGENTS.md components convex lib hooks app extraction-worker imessage-worker
```

Prefer extending the existing primitive when the meaning matches. Add a new primitive only when reusing the old one would make the API misleading. If a change creates, deletes, renames, or materially changes a primitive, update both `AGENTS.md` and this skill in the same PR.

## Catalog

Sidebar edits use `useLocalFirstAutoSave` and `AutoSaveStatus`; omit manual Save and footer Close/Cancel buttons when the sidebar already has a close control. Creation and consequential workflow actions remain explicit. Record-keyed editors keep drafts scoped when switching rows. Client Files also uses sidebar editing and footer preview/download/archive/restore actions; sidebar settings tables may contain switches. Procurement file sharing uses a compact audience table with Client visibility and Broker visibility switches; broker visibility includes the attachment in the next shared packet snapshot, while outreach-bound files remain excluded. `components/ui/file-download-button.tsx` owns storage-file downloads that fetch a blob before saving, preserving the open app and sidebar across cross-origin storage URLs.

- `convex/operatorGoogleWorkspace.ts`, `convex/actions/operatorGoogleWorkspace.ts`, and `convex/lib/googleWorkspace*.ts` own the global operator-only Google Workspace Gmail connector. Channels → Google Workspace configures manual mailboxes or Directory discovery and verifies access; `GOOGLE_WORKSPACE_SERVICE_ACCOUNT_JSON` remains a backend environment secret. Domain-wide delegation uses `gmail.readonly`, with `admin.directory.user.readonly` only for Directory discovery. Every active operator may read all configured mailboxes without per-mailbox ACLs; tenant agents and tenant MCP never expose the connector. The existing operator registry/audit/attachment path serves live mailbox discovery, email search, thread reads, and original attachment retrieval across operator web, Slack, iMessage, and MCP. Registered calls return short operator/thread/tool-bound continuation references backed by the existing lossless action audit; raw signed Google cursors stay in backend audit output and still bind settings, credentials, query, and page size. Results preserve mailbox provenance, bounded continuation and partial failures; no Gmail writes, automatic ingestion, or search index is created. Setup and troubleshooting live in `docs/deployment/google-workspace.md`.
- Operator confirmation contention: each operator thread permits one live exact confirmation. Competing direct or MCP registered writes return the active confirmation as a structured block and terminate their own runs without executing; pending operator approvals have no time limit and remain live until approved, cancelled, or superseded. Approval reruns the shared reference preflight before execution. The optional legacy `expiresAt` field is retained for stored rows but is ignored for pending approvals; previously scheduled expiration jobs are harmless no-ops. Already-expired confirmations stay terminal, and clicking an expired or inactive Slack control replaces it with a recovery instruction.

- Spot branding: `components/ui/spot-wordmark.tsx` owns the canonical browser Redaction wordmark and mirrors the `clarity-landing` lockup at a default 1.25rem wordmark, one-em globe, and quarter-em gap (20px, 20px, and 5px by default). Browser surfaces reuse that component and scale it through its text size so the mark, text, and spacing stay proportional. `lib/spot-social-image.tsx` and its local artwork under `public/spot/` own the generic Open Graph card and stay visually matched to the `clarity-landing` `spot.insure` social image, including its watercolor field, ASCII pattern, and wordmark; the product card intentionally identifies `app.spot.insure` in Geist. Product titles and sender names are simply `Spot`; when publisher attribution is present, it is exactly `from Tools for Enlightenment`.
- `convex/lib/channelAgentRunner.ts` owns shared authenticated conversational turn execution, tool-outcome auditing, and the policy-evidence contract. Web, Slack, email, iMessage, and MCP keep ingress, authorization, persistence, typing, reactions, and delivery channel-local. Policy-dependent informational answers must complete a current-turn policy lookup; the runner may retry once with read-only policy tools only when every earlier tool is replay-safe, never after an unknown or side-effectful tool.
- `convex/operatorAgent.ts`, `convex/operatorAgentRunner.ts`, and `convex/lib/operatorAgentToolRegistry.ts` own the internal operator-agent primitive. Operator threads, messages, runs, and exact confirmations use their dedicated tables rather than tenant conversation tables. A portal thread captures its first available page context as immutable origin context and injects that same routing hint into every later run; page navigation never replaces it. The registry is the only place to add operator capabilities: every tool declares input schema, effect, execution boundary, required role, target, and confirmation policy; non-read execution goes through the fingerprinted confirmation and idempotent audit ledger, then resumes the same goal after approval. Action-backed tools revalidate the active operator and exact fingerprint at their asynchronous boundary. Shared rich policy, compliance, and client-file action tools execute through `convex/actions/operatorAgentRichTools.ts`; exact policy writes still require registry confirmation. `search_organizations` includes legacy client rows and bounded human-name typo tolerance while returning exact IDs for downstream tools. Exact confirmation summaries resolve persisted target names or titles before reaching a human surface and never expose storage IDs; portal confirmations stay anchored to their prompting message as durable pending, approved, cancelled, expired, superseded, or unavailable timeline artifacts, with `PillButton` controls only while the current operator can act, while Slack uses signed, actor-bound Confirm/Cancel Block Kit controls and removes them after the decision. Operator history search is exact-thread bounded, and older attachments may be reopened only when already registered to that thread. `lookup_address` reuses permanent Mapbox validation, while exact-confirmed `generate_coi` reuses the shared certificate batch generator and registers successful PDFs as protected operator-thread attachments before resuming the goal. Long goals checkpoint bounded tool evidence and reschedule another segment instead of treating the model step ceiling as completion. `convex/lib/agentAttachmentContext.ts` converts bounded PDF, image, XLSX, CSV/TSV/text, JSON/XML, DOCX, and PPTX files for the current model turn, while every channel stores and ownership-binds its files before enqueue. Portal blobs use expiring operator-owned upload intents and are removed when unconsumed; `convex/lib/operatorMcpToolCatalog.ts` is the single role/scope-filtered MCP `tools/list` projection of the registry. Keep the operator, client-agent, subagent, and MCP inventory in `AGENT_TOOLS.md` synchronized with every tool or material metadata/availability change. `convex/lib/operatorAgentIntentRegistry.ts` is the single versioned owner for operator task launchers: the server resolves applicability from immutable page context, renders the visible opening objective, and records intent ID/version as a structured message artifact before the ordinary governed run begins. The operator portal rail and dedicated `/operator/threads/[id]` pages reuse one operator conversation renderer plus the client thread's `ThreadMessageBubble`, `AgentThinkingBubble`, attachment chip, and `ChatInputOverlay` primitives; rail density is a shared overlay variant rather than a second composer. `/operator/threads` is the default operator entry and owns task starters plus the tabular Active/Archived inbox; archive and restore are reversible operator-authorized thread transitions, and new activity automatically returns an archived thread to Active. The operator sidebar shows active cross-channel threads, while the rail's recent-task picker shows only active portal threads whose immutable origin shares the current page scope. Internal Slack, the separate operator iMessage lane, and operator MCP are adapters to this core and must revalidate an active operator identity before enqueue, read, cancel, or confirm. The `operator_agent` model route is a required explicit direct-provider selection, excluded from cl-router settings and automatic fallback; missing or unavailable configuration fails the run closed.
- `convex/clientFiles.ts`, the generated-API-free shared invariants in `convex/lib/clientFiles.ts`, `convex/actions/clientFileNaming.ts`, and `components/client-files/client-files-workspace.tsx` own the per-client miscellaneous-file dropbox across UI, operator-agent, and procurement ingestion paths. Operators alone upload or mutate these files; client and broker reads include only explicitly visible active rows, policy links must target the same active client, and upload intents expire and clean up unreferenced blobs. Shared tenant-agent lookup/read/attach tools and direct tenant MCP list/get tools must remain visibility- and readable-scope-filtered; operator registry reads may include private active rows only after current-operator revalidation. Initial naming reuses bounded attachment parsing (LiteParse first for PDFs) plus `classification` or `chat_vision`, treats the upload hint as untrusted context, and must lose to a manual rename. Every active stored file also schedules `convex/actions/companyInformationExtraction.ts` through the source ledger in `convex/companyInformation.ts`: reuse the bounded parser, use `document_extraction` for readable document content or `chat_vision` for images, fill high-confidence source-backed organization profile fields before durable `orgWikiSections` fallback, retain exact contributor refs, reject stale completions, and remove that source's contributions on archive/delete. Restore schedules a fresh pass. PDFs use the app PDF panel, images use the right panel, and other formats download. Operator-agent adds file privately without a confirmation, operator-agent edits remain exact-confirmed because they can expose a file to the client, and an add may consume only an attachment already bound to that operator thread, by exact storage ID or exact filename. Files default to private.
- Procurement projects are jointly owned by `convex/procurementRequests.ts`, `convex/clientProcurementRequests.ts`, `convex/procurementPacket.ts`, `convex/procurementProposals.ts`, `convex/proposalExtraction.ts`, `convex/brokerProfiles.ts`, and `components/procurement/*`. Extend these owners instead of creating proposal-shaped policies or exposing operator request details to clients. Client reads use only the allowlisted client DTO. A request stores one intake `narrative`, which seeds its client-visible packet section; other submission content belongs in audience-scoped packet sections. Outreaches require a real broker and preserve immutable contact and sent-packet snapshots. Proposals, documents, source evidence, reviews, comparison, packets, broker activity, imported email, and internal files remain operator-private and absent from tenant MCP. Proposal documents use the separate proposal queue/source tables while sharing the extraction worker's global admission and serialized LiteParse lane; never weaken the bound-policy quote rejection gate. Reviews compare the broker-visible packet with evidence-tagged proposal Markdown, bind the extraction fingerprint and packet revision, and selection revalidates the current staff confirmation atomically. Stable model task kinds are `requirement_extraction` for intake, `extraction` for proposal documents, and `analysis` for proposal review. The operator registry/operator MCP must retain exact-confirmed parity for request, packet, proposal, outreach, file, email, and broker-profile operations.
- Procurement correspondence reconciliation stays inside those owners. Forwarded attachments use canonical private `clientFiles`; exact participant/contact matching may suggest an outreach, but ambiguity requires an operator selection. Web and Spot-agent filing reuse the same replay-safe proposal command, preserve the source email thread in the request audit, and never expose private procurement records through tenant email or tenant MCP.
- Broker packet browser sharing is available from Packet and Proposals navigation in `components/procurement/procurement-request-workspace.tsx`; request creation schedules the initial shared link. `components/procurement/packet-workspace.tsx` owns packet display and direct Markdown editing; browser autosaves commit sections atomically against the last acknowledged packet revision and preserve drafts across subscription updates. Proposals rows open one broker sidebar with PDFs and Terms tabs for uploads, contact/log edits, extracted content, and packet review; all record actions use its footer. Sidebar upload progress uses toasts, and field-specific autosaves preserve live extraction/status updates. One request-wide link serves the same shared packet to every broker; replacing it revokes the prior active request-wide link, and its secret is exposed only for immediate copying. The shared snapshot may include request-wide released files only. Outreach-bound files remain private to that outreach and can never enter the shared preview, snapshot, public read, or download paths. Legacy outreach-bound links remain readable and revocable only.
- Agent-led procurement setup creates standalone clients only through the exact-confirmed `create_client_organization` command and the existing `operator.createSoloClientInternal` owner. It provisions no users, returns the exact client ID for request creation, rejects exact-name duplicates, and fails closed during operator impersonation.
- Exact-confirmation preflight: `convex/lib/operatorAgentConfirmationPreflight.ts` validates every exact-confirmed operator tool before a confirmation is requested: exact IDs, client or broker ownership, cross-record links, policy activity, extraction state, source spans, feature-flag eligibility, dates, emails, and URLs. A bad reference fails as a correctable tool error instead of consuming an approval; the write still revalidates the same rules inside its transaction, idempotent replays of completed actions skip the preflight, and a registry test fails when an exact-confirmed tool has no preflight case.
- Thread identity metadata belongs only to user messages: `UnifiedMessageBubble` and the operator conversation renderer show avatar, sender, channel, and timestamp on user rows, while agent responses, thinking states, and confirmations render without an agent identity row.
- `convex/threadActionConfirmations.ts`, `convex/lib/threadActionConfirmationValidators.ts`, and `convex/lib/actionConfirmationFingerprint.ts` own exact, actor-bound, task-epoch-bound confirmations without time-based expiry. Email send/cancel, draft snapshots, multi-COI delivery, and requirement import must consume these records or a separately typed persisted authorization at the final side-effect boundary. `convex/lib/emailSendIntent.ts` owns same-turn single-recipient email authorization: it conservatively recognizes an affirmative send direction and binds the persisted source message to the current actor, organization, and thread; negated, advisory, quoted, older-turn, ambiguous-recipient, and multi-COI actions still require their ordinary confirmation path.
- `convex/lib/threadMessageValidators.ts` and `convex/threads.ts` own structured message kinds, source identity, exact dedupe keys, and parsed email metadata. Missing kinds mean visible legacy conversation; browser components never infer workflow status, channel sync, quotes, or duplicates from prose.
- `convex/lib/inboundEmailParser.ts` owns bounded reply/forward parsing and connected-mailbox HTML-to-text conversion; `convex/lib/forwardReplyDirection.ts` owns the structured decision required before an internal forward may target its parsed original sender.
- `convex/lib/confidence.ts` and `convex/lib/transportRenderers.ts` own cross-runtime confidence compatibility and parsed transport rendering. Email uses `marked` plus strict `sanitize-html`; completed Slack answers always use Convex-rendered mrkdwn Block Kit sections, including when stopping an existing stream, and its worker passes those values through without semantic formatting. iMessage keeps its parser-native renderer and exact legacy-trailer boundary scrubber.
- `convex/lib/searchTokenizer.ts` is the Unicode-normalizing tokenizer owner for retrieval, policy/compliance lookup, thread history, and organization memory ranking.

Certificate holder edits in the shared detail drawer target the selected certificate explicitly, regenerate its PDF, and commit holder changes only while recording the next immutable version. Do not route changed holder identity through ordinary reuse matching or mutate an issued PDF in place.

- Browser typography: `lib/typography.ts` is the only browser typography owner. React surfaces select closed semantic roles with `typeStyle`; Redaction cuts use `redactionTypeStyle`, and external/inline exceptions use the owner's named adapters. Raw typography utilities and inline font properties are prohibited under `app/` and `components/`. Keep heading semantics independent from visual roles and follow `docs/design/typography.md`; generated social images, transactional email HTML, and PDF output keep separate contracts.
- Appearance controls: `components/ui/theme-mode-selector.tsx` owns the shared Light/Dark/System selector used on the customer/admin `/profile` page and operator `/operator/profile` page. Appearance controls belong on those profile surfaces rather than in app navigation.
- App shell and navigation: `components/app-shell.tsx`, `components/app-shell-sidebar-layout.tsx`, `components/app-shell-panel-layout.tsx`, `components/app-sidebar/*`, `components/app-sidebar/nav-config.tsx`, and `lib/settings-sections.ts`. Route families that replace the sidebar menu keep one shell mounted at their shared layout boundary; `/clients` owns its shell in `app/clients/layout.tsx`, while `AppSidebar` swaps main and client-detail menus inside the stable rail with a clipped, reduced-motion-aware transform/opacity transition. Custom app-shell left sidebars resize from 180px to 360px, retain a 220px default, collapse to the 56px mini view below the expanded minimum, and persist width/collapse state under the signed-in user ID. App-shell detail, entity, and PDF panels resize through `components/ui/resizable.tsx` and persist their user-resized layouts under the authenticated sync user; panel contents fill their parent rather than owning drag state. Route and action rows use `SidebarMenuItem`, and sidebar trees use `SidebarTooltipProvider` for shared tooltip timing, styling, keyboard hints, and cursor behavior. Navigation sequences use `navShortcut`; direct command shortcuts use `commandShortcut` for platform-aware Command/Ctrl hints. Settings page groups, role-aware tabs, legacy aliases, and canonical destinations live in `lib/settings-sections.ts`; Integrations and Mailboxes are separate destinations, while Beta owns org beta flags, onboarding reruns, and admin-only destructive organization reset.
- Operational UI: `components/ui/operational-panel.tsx`, `components/ui/form-section.tsx`, `components/ui/operational-toast.tsx`, `components/ui/status-tag.tsx`, `components/ui/table.tsx`, `components/ui/auto-save-status.tsx`, `components/ui/address-autofill-input.tsx`, `components/ui/message-meta-tag.tsx`, `components/ui/pill-button.tsx`, `components/ui/resizable.tsx` for accessible adjacent panel groups, `components/ui/select.tsx`, `components/ui/dropdown-menu.tsx`, `components/ui/searchable-select.tsx`, `components/ui/smooth-corners-provider.tsx`, `lib/smooth-corners/*`, `components/settings/settings-switch.tsx`, `components/settings/feature-flag-toggle-row.tsx`, `components/settings/team-members-list.tsx`, `components/prose-markdown.tsx`, and `lib/date-format.ts`. `OperationalLabelValueRow` stacks its label and value at mobile widths, uses a smaller mobile label, and applies optional right alignment only from the two-column breakpoint so stacked values remain left-aligned. `FormSection` owns structural headings, optional descriptions/actions, and the divided spacing rhythm inside editable forms and drawers; individual field labels remain small and muted. Operational toasts keep a stable intrinsic height while mounted because Sonner does not remeasure component-local collapse state; descriptions stay on one line, a single action stays inline, and multiple actions wrap below. The shared toast shell uses one subtle floating-layer border and shadow and stays visually static on hover; hover feedback belongs to explicit controls inside the toast. `StatusTag` owns compact semantic state labels with neutral, info, success, warning, and danger tones; roles, categories, provenance, and other non-status metadata stay on `Badge`. `Table` owns table overflow and applies the shared theme-aware `.table-scrollbar`; shared tables size to their unwrapped content with at least full container width and use 16px horizontal by 12px vertical body-cell padding. Raw table wrappers use `.table-scrollbar` rather than native scrollbar chrome. `TeamMembersList` owns team roster rows and an optional per-member action slot inside the Access cell. `ProseMarkdown` owns finalized-message rendering. Web chat processing uses one thinking bubble and never renders partial text, reasoning, tool activity, subagent logs, or confidence annotations. Select and dropdown popup content stays on the shared portaled overlay path so overflow containers cannot clip it. The smooth-corners provider owns app-wide Lisse continuous geometry: it preserves true circles, redraws supported uniform borders, suppresses transitions while reading and applying managed geometry so redraws cannot capture the engine's transparent native-border state, refreshes managed geometry after root theme changes, converts compatible always-on outer rings and shadows, releases clipping around active focus rings or escaping content, conservatively leaves dense containers unclipped when the bounded descendant escape scan is not exhaustive, and reversibly opts whole subtrees out with `data-smooth-corners="off"`; do not wrap individual rounded elements or add another global corner engine. Nested editors aggregate into one page-level save status. Human-facing absolute dates use `MMM D, YYYY` through the shared date-format owner; `formatDisplayPolicyPeriod` renders an effective date without an expiration date as `Ongoing from {date}`, while `policyTermType: "continuous"` renders as `Until Cancelled` regardless of a stored expiration placeholder. Every policy-period surface, including the Extraction tab, passes the term type to that formatter. Native date inputs, storage/API values, relative ages, and time-only diagnostics keep their domain-specific formats.
- Slack transcript rendering: `components/prose-markdown.tsx` owns finalized browser rendering and its `sourceFormat="slack-mrkdwn"` mode normalizes Slack quotes, lists, emphasis, links, and references through `lib/slack-mrkdwn.ts` before the shared Markdown pass. Use that mode only for Slack-authored source messages; stored agent answers remain Markdown.
- File intake UI: `components/ui/file-drop.tsx` owns the shared drag/drop and picker treatment. Use `onFile` for one file and `multiple` plus `onFiles` for batches; the domain surface owns upload limits, authorization, upload intents, and registration.
- App top-bar actions: `components/app-top-bar.tsx` owns the single action row, spacing, and inherited compact `PillButton` size. Keep status tags in the overview or active-tab body. Top-bar actions use only primary and secondary treatments: order secondary utility, view, recovery, and reversible archive actions before at most one create or generate primary action at the right edge. Primary actions use `PillButton` with a persistent icon and label. Compact secondary actions may use `variant="secondary"`, `expandLabel`, a nonempty `label`, and an icon child so labels appear on keyboard focus or fine-pointer hover while coarse pointers retain accessible icon-only controls. Do not mix label-only and icon-bearing buttons in a top-bar action group, and reserve expanding actions for app chrome rather than dense close, remove, or structural controls. Follow `docs/design/interface-style.md`.
- Client Agent settings keep Email, iMessage, and Slack under one Agent channels destination backed by `components/settings/agent-channels-section.tsx` across client, broker-client, and operator-client surfaces. The page shows a compact channel-status list and opens the selected channel in the shared right-side settings drawer. The Email drawer is the only settings owner for the user-facing agent email address: managed clients inherit their broker's `organizations.agentHandle`, standalone clients use their own handle or the shared `agent` default, broker admins edit their broker-owned address, and operators may edit only standalone-client addresses. Operator client creation does not assign an address, and generic operator broker details do not edit one; broker provisioning or onboarding may seed the initial broker address before Channels owns later changes. Keep this routing identity on `organizations`; `agentChannelSettings` remains the per-client availability and automation owner. Broker Agent settings expose their address through Channels, while Behavior owns agent action policy rather than channel identity. An unfinished client Slack connection opens an operator-only, persisted four-step setup or reinstall flow in that drawer; clients see read-only progress, legacy valid connections remain complete, and explicit Finish returns the drawer to the compact workspace, support, channel, and automation view. Legacy direct-channel destinations resolve to Channels, and Slack OAuth returns there.
- Destructive actions: `PillButton` is the only destructive button owner. Internal hrefs use Next client navigation with full prefetch by default; external URLs, protocol links, and downloads retain native anchor behavior. Use `variant="destructive"` for both triggers and confirmations; icon-only triggers add `iconOnly` plus a nonempty `label`, keeping destructive intent separate from shape. Do not use `Button`, raw buttons, or custom red classes for destructive actions. Editable-list removal and reversible archive controls may remain quiet icon actions when destructive emphasis would misstate their impact.
- Phone input: `components/ui/phone-input.tsx` owns country selection and E.164 normalization. Calling codes stay in the country button, the field renders national digits only, country changes preserve those digits, and pasted international numbers select the matching country.
- Auto-save feedback: `AutoSaveStatusProvider` in `components/ui/auto-save-status.tsx` owns the single app-wide bottom-center save surface. `AutoSaveStatus` instances register with that provider instead of rendering inline. The expected `saved` state stays hidden, ordinary `saving` stays hidden for three seconds, and `unsaved` or `error` takes priority and remains visible until the changes become safe. Keep save errors on the hook-owned deduplicated error toast; do not add success toasts or page-local status labels.
- Attachment tags: `components/agent-thread/thread-attachment-chip.tsx` owns compact attachment rendering and stored or on-demand PDF preview behavior, including connected-mailbox attachments resolved only when opened.
- Transactional email rendering: fixed email templates use `buildEmailShell` from `convex/lib/emailTemplate.ts` plus its semantic `spot-email-*` classes for text, surfaces, dividers, links, and actions. The default header uses the official Spot lockup and the footer attribution is exactly `from Tools for Enlightenment`; white-labeled headers retain the organization's own mark and name. The shell owns light/dark color-scheme metadata, `prefers-color-scheme` rules, Outlook `data-ogsc`/`data-ogsb` overrides, and legacy `bgcolor` fallbacks. Do not create standalone fixed-template documents or declare a light-only scheme; agent-authored free-form reply HTML remains outside this fixed-template contract.
- Branding: rendered app/org marks live in `components/ui/brand-icon.tsx`, `components/ui/org-brand-icon.tsx`, and `components/ui/logo-icon.tsx`; neutral browser theme and adaptive border tokens live in `app/globals.css`; browser brand tokens and color utilities live in `lib/branding.ts`; Next.js server-only viewer metadata lives in `lib/viewer-branding.ts`; Convex-safe branding context and white-label gates live in `convex/lib/branding.ts`; Convex org-logo URL attachment lives in `convex/lib/orgBranding.ts`; shared email shells live in `convex/lib/emailTemplate.ts`; notification-email composition lives in `convex/lib/notificationEmailTemplate.ts`. `policies.carrierIdentity` is the only consumer-facing carrier contract. It stores the source designation, preferred display and operating names, public-name relationship, every legal entity with provenance, and the official-site branding snapshot. Keep `carrier`, `carrierLegalName`, `security`, and `insurer` only as compatibility projections; never collapse multiple legal entities or Lloyd's lead/syndicate wording into a generic name, and never infer a General Agent from DBA evidence. `convex/lib/carrierIdentitySource.ts` owns ranked bounded-span/source-node evidence, bbox-aware same-column reconstruction, authoritative source rebuilds, and current-version brand preservation for an unchanged source identity; accept an operating-name/DBA or Lloyd's led-by clause only when its legal, operating, or Lloyd's identity matches a source-backed `carrier` or `insurer` party. Fallback identity selection considers only source-backed carrier/insurer parties, preserves every complete legal entity represented by those parties, and never attaches insurer-value provenance to a differently named party. Carrier backfills preserve structured insurer details only when the old insurer name exactly matches the rebuilt primary legal entity; otherwise they clear stale fields and rebuild only address/NAIC values from the matching source-backed party. A failed rebuild may retain the stored identity only when the current compatibility carrier still positively matches its complete source designation; a missing, unknown, or conflicting carrier clears the identity, enrichment state, and legacy cache rather than reattaching stale carrier data. Terminal extraction awaits `convex/actions/enrichCarrierIdentity.ts`, researches the complete source designation plus a concise carrier alias, and uses the triage model to judge official-site identity from first-party evidence. Deterministic checks are limited to candidate safety and grounding: a model-selected public site must not be a login or redirect, must include identity evidence, and any public name must be visible on the selected site. Token overlap, exact-word agreement, acronyms, and domain-name similarity never decide carrier identity. The action persists branding inside the same identity. Cache application and reusable lookup evidence are keyed only by the complete canonical source designation, never by a constituent legal entity or compatibility alias. If the source identity changes during either successful cache application or a failed lookup, the stale attempt releases its lease and schedules enrichment for the current identity. `carrierBrands` is internal official-site lookup-cache plumbing only; policy queries and UI must not expose it or trigger enrichment on read. Repair final policies through `migrations:runCarrierIdentityBackfill`, which records eligible rows as pending, schedules one action per policy, pages source spans and nodes outside migration transactions, rejects a policy changed during paging, applies a compact final patch, schedules ordinary enrichment, and reports rebuild outcomes. Wait for both the migration runner and `pending: 0`; explicitly re-extract only rows reported without usable stored sources. `convex/lib/websiteBrand.ts` owns site signals, resilient canonical/domain favicon recovery, favicon storage, and dominant non-neutral favicon/logo color extraction with favicon, theme-meta, stylesheet, or HTML provenance; no neutral fallback is persisted as carrier data; every fetch rejects non-global IP/DNS answers, pins one validated DNS answer for the connection, revalidates redirects, streams through hard byte limits, and bounds PNG dimensions before decode. `convex/lib/policyPartyContext.ts` owns the visible carrier projection shared by policy cards, references, and agent-tool policy output: broker insurer overrides replace the extracted display name and suppress source branding and structured identity unless the visible carrier still matches the canonical identity. Policy names prefer that carrier/insurer projection and use the General Agent only when no carrier identity is available. `lib/policy-card-branding.ts` owns carrier-toned surfaces and the softly masked policy pattern shared across policy cards, Policy Overview, iMessage previews, Open Graph images, and web-thread policy mentions. That pattern is a static, carrier-phased snapshot of Clarity Landing's `FooterAsciiShader`: it reuses the brand glyph ramp and wave/noise field while avoiding per-card WebGL contexts and distracting animation on repeated operational surfaces. When no carrier accent is available, browser surfaces use the white light canvas or non-black dark canvas and its foreground while retaining the pattern; branded Policy Overview uses its separate accessible light carrier tint. Interactive browser policy surfaces keep one quiet surface-appropriate border, never use card or hover shadows, and use a fine-pointer color overlay instead of stacking a hover border. Product lines remain separate values. Certificates intentionally retain their neutral identity treatment.
- Carrier evidence and icon safety: a generic source-backed `Lloyd's Underwriters` party authorizes a led-by clause only through overlapping source-node or source-span provenance on bounded, non-page-level evidence; exact lead/legal matches remain valid without the generic shortcut. Favicon fetches return and store only dimension-safe PNG or ICO payloads, validating embedded PNG IHDR or ICO bitmap dimensions before creating the blob.
- Carrier enrichment retry state belongs to the canonical source identity. When terminal extraction rebuilds a different source identity, clear its prior enrichment status, attempt count, and lease timestamp before scheduling research for the new carrier.
- Carrier identity backfill approval uses the read-only `actions/backfillCarrierIdentity:audit` action, paging every policy and its stored evidence without writes and returning actual proposed set/unset changes; do not use migrations-component dry-run as decision evidence because it rolls back scheduled rebuild work. Write actions retry transient evidence-paging and apply failures with bounded backoff; `carrierIdentityBackfill:retryPending` is the durable operator recovery path for stranded pending rows, and completion requires both the migrations runner and `pending: 0`.
- Carrier identity backfills retain any nonempty source-backed insurer address in the compatibility projection, including formatted-only and locality-only operational addresses; `street1` is not required when another canonical address component is present.
- Carrier identity backfills validate every operational-profile insurer/party provenance ID against the source nodes and spans loaded for that policy before rebuilding identity or copying address/NAIC fields; dangling legacy IDs are not evidence.
- Stored-source ACORD taxonomy and carrier backfills admit only stable final policies. A missing legacy pipeline status or explicit `complete` may be eligible; explicit `running`, `paused`, `idle`, or `error` is ineligible even when `extractionDataStage` temporarily remains `final`.
- Feature flags: `convex/lib/featureFlags.ts`, `organizations.featureFlags`, `orgs.setFeatureFlag`, `operator.setClientFeatureFlag`, `isFeatureEnabled`, and `/settings?section=beta`. `coverage_recovery_v2` is a default-off client-only flag resolved from the policy-owning client org; managing brokers do not supply or inherit it. `imessage_app_cards` is default-off for broker and client orgs and gates public policy/certificate link minting plus delivery at the iMessage boundary.
- Local-first sync and current org: `lib/sync/use-cached-query.ts`, `lib/sync/spot-cached-queries.ts`, `lib/sync/operator-cached-queries.ts`, `lib/sync/use-local-first-auto-save.ts`, `lib/sync/auto-save-sequencer.ts`, `hooks/use-current-org.tsx` for the canonical lightweight viewer-org `useCurrentOrg`, and `lib/hooks/use-active-org-context.ts` for URL-aware selected-org surfaces. `useLocalFirstAutoSave` is the sole save-on-change primitive; validation-aware saves use `enabled`/`canSave` and custom timing uses `delayMs`. Settings text and address inputs keep `autoSave` disabled and call `saveNow` after blur; `AddressAutofillInput` forwards `onBlur` and reports completed Mapbox values through `onRetrieve`. Availability-checked inputs pause auto-save while focused and resume after blur, while discrete selects flush after selection. `applyLocal` synchronizes shared cache only after the current backend intent succeeds, and dependent actions await `saveNow`. The shared sequencer keeps backend flushes in intent order, while the hook suppresses stale value/reset callbacks; `canSave` gates permission and validation instead of dirty state, and identity or availability fields use current raw input rather than debounced validation input for `valueKey`. Auto-save writes bypass the durable outbox so stale form intents cannot replay after reload; failed writes require explicit retry. Nested editors report into one page-level `AutoSaveStatus`; completion stays inline and the hook owns the deduplicated error toast, so do not add duplicate Saved indicators or success toasts. Reserve `saveNow({ force: true })` for derived legacy state that began as the local baseline but still needs persistence. Do not add page-local debounced save effects.
- Auth/access: `convex/lib/access.ts`, `convex/lib/operatorIdentity.ts`, `convex/lib/userFacingErrors.ts`, `lib/user-facing-error.ts`, `convex/lib/apiAuth.ts`, `convex/oauth.ts`, Convex HTTP OAuth handlers, and `convex/lib/threadAccess.ts`. User-callable authentication, role, read-only, and operator failures use the structured backend error contract; client catches and auto-save surfaces use the shared formatter so raw Convex request-ID/server wrappers never reach users. Current-org admin mutations use `requireCurrentOrgAdminWrite`; keep `requireCurrentOrgAdmin` read-only so live operator impersonation can render admin-scoped queries without permitting writes. Impersonated organization writes call `assertImpersonatedSetupWrite` before costly work or persistence; only onboarding impersonation may perform impersonated setup writes. Operator impersonation teardown keeps its target operator return route in `lib/operator-impersonation-stop-state.ts` until `AuthGuard` reaches it, so the no-session fallback cannot overwrite client-detail or broker-detail navigation. The operator client-support page may edit organization profiles and team access only through the explicit `operatorClientOrgId` target scope in `convex/orgs.ts`; those writes require an active operator profile and record operator audit events without granting write access to live impersonation. Operator OAuth requires an exact `/mcp` resource audience and revalidates the active operator profile; tenant REST routes must reject operator tokens even when a caller supplies an organization header.
- Model routing: the separate `claritylabs-inc/cl-router` service owns policy, provider execution, failover, telemetry, explicit human-rating scoring, and operator controls; exact-pinned `@claritylabs/cl-router-policy` remains the shared task/model vocabulary and static safety envelope. Spot entry points are `convex/lib/clRouterClient.ts`, `convex/lib/clRouterLanguageModel.ts`, `convex/lib/models.ts`, `convex/lib/sdkCallbacks.ts`, and `extraction-worker/src/clRouterClient.ts`. `CL_ROUTER_TASKS` gates staged rollout, while authenticated `query_reason` and extraction-family tasks use cl-router whenever its URL and inference secret are configured and keep a reserved cross-provider fallback even when a persisted policy or caller snapshot is stale. The Models tab defaults task routes to Automated routing; explicit selections carry a persisted override marker and enter cl-router as request pins, while fallback remains a separate route. Authenticated agent surfaces share an 8,192-token output ceiling. Candidate exhaustion may switch the entire run to direct execution only before Spot observes text or a tool call; never change transport after visible output or tool execution. A blank or truncated no-tool response may use the configured fallback, while a turn with completed tools gets one tool-free continuation over the existing results so side effects are never replayed. Forced `required` and exact named-tool choices remain protocol-level validation, not a business-tool registry. Quality feedback is explicit only: web and Slack use immutable thumbs controls, eligible direct iMessage responses receive a deterministic occasional prompt, and operator extraction reviews target a verified request. `threadMessages.routerRequestId` and extraction run/trace telemetry own attribution; comments and corrections stay in Spot, while cl-router receives only up/down. Never infer quality from tool completion, workflow success, provider failure, fallback, schema validity, or other technical outcomes. Keep model step caps separate from latency budgets. Use the shared org/public agent and text/object helpers with stable task-kind, session, trace, and channel metadata. Keep compatibility in the policy/router service; `modelRoutingEvents` owns direct and routed run, step, fallback, error, incomplete-output, finish-reason, token-cap, visible-text, reasoning-token, tool, workflow, router-owned request ID, and sanitized failure-attempt telemetry. `requirementExtractionRuns` owns compliance-import parser, route, usage, counts, duration, and error telemetry shown on `/operator/telemetry`. Labels stay in the centralized model catalog. cl-router and retained break-glass call providers directly; never add another inference gateway.
- Routed query challengers: `query_reason` shares one bounded exploration budget across OpenAI GPT 5.6 Terra, Fireworks Muse Glimmer 30B, and Fireworks Inkling. Keep these reviewed static challengers in `@claritylabs/cl-router-policy`; `convex/lib/modelCatalog.ts` owns only their Spot display labels and explicit model-selection entries. Extraction keeps its separately reviewed challenger.
- Extraction/source evidence: `extraction-worker/`, `convex/actions/policyExtraction.ts`, `convex/lib/extraction.ts`, `convex/lib/pipelineMutations.ts`, `convex/lib/sourceTree.ts`, `convex/lib/carrierIdentitySource.ts`, `convex/lib/policyDocumentStructure.ts`, `convex/lib/extractionPostProcess.ts`, `convex/lib/documentMapping.ts`, `convex/lib/policyPeriodExtraction.ts`, `convex/lib/valueNormalization.ts`, `convex/lib/declarationFacts.ts`, `convex/lib/orgProfileFacts.ts`, `convex/lib/policyPartyContext.ts`, `convex/lib/entityTypes.ts`, `convex/lib/coverageBreakdown.ts`, `convex/lib/coverageNames.ts`, `convex/lib/coverageScoping.ts`, and `convex/lib/sdkCallbacks.ts`. `sourceSpansForSdk` owns stored-Spot-span conversion into the SDK contract. The worker may process full and provisional jobs concurrently, but `convertPdfWithLiteParse` serializes the native parser across full, preview, and authenticated HTTP conversions so parallel native instances cannot abort the process. The priority queue limits each higher-class burst to four starts so HTTP remains responsive without starving preview or full work. HTTP conversion attaches upload, response, and socket cancellation before body parsing so disconnected callers are removed from the queue. Convex attempts synchronous LiteParse conversion once per preprocessing path and falls back directly to the PDF.js-only helper after failure or timeout. The bounded preview allowlist retains a source-stated carrier product/plan as `programName` until final extraction replaces it with source-backed `productIdentity`. The bound-policy gate auto-archives only rejected new-upload rows. A re-extraction, renewal, or full-retry failure before replacement promotion keeps both the bound policy and canonical extraction run operationally complete and retains the compact checkpoint including its staged `fileId`. Resume continues that checkpoint, Restart reseeds the staged file from phase one, and a full/header re-extraction always seeds from the active policy file and discards the staged source. The failed attempt remains in extraction trace history; successful completion and explicit cancellation clear retry state, while post-promotion failures fail closed. Enabled `coverage_recovery_v2` jobs use the SDK's `extraction_coverage_recovery` pass before cleanup; only versioned SDK success suppresses Spot deterministic recovery. `coverageScoping` remains the fallback for disabled clients, old results, and model failure. Operator backfill calls `runCoverageRecovery` over stored spans/nodes without reparsing. Both recovery paths remove premium/exposure values from coverages, separate premium, taxes, fees, and total payable, and store referenced auto/property/location schedules in `coverageSchedules` with source provenance; scheduled assets do not belong in description of operations. Every recognized extracted date destination, including nested profiles, coverages, forms, endorsements, declarations, and supplementary facts, passes through `normalizeExtractedDateFields` at the mapping and persistence boundaries and stores parseable dates as `MM/DD/YYYY`. Organization profile data is the editable current company record; each policy separately preserves its extracted insured name/address, operations, additional named insureds, producer, insurer, and General Agent. Canonical extraction uses role `general_agent` and structured field `generalAgent`, while legacy `mga` and `administrator` inputs remain read-compatible. Insurer NAIC numbers and producer/General Agent license numbers stay attached to the same source-backed party identity; a carrier party may supply address or NAIC data for a single resolved legal insurer only when their normalized names match exactly. Source-backed party/declaration extraction remains immutable, while broker corrections live in `policies.policyDetailOverrides`. Policy Overview owns core metadata plus a divided description-of-operations row; the overview and each party card expose broker-only Edit actions for final policies and open the shared right-panel drawer with typed date, currency, phone, email, and Mapbox address controls. Cards retain content-width breakpoints of one column below 48rem, two columns from 48rem, and three from 64rem; coverage schedules live in the Coverages tab. `policyPartyContext` owns manual-override, structured-policy, compatibility, and legacy precedence and limits client-profile fallback to insured address and operations. `coverageBreakdown` preserves coverage provenance while owning grouped LOB schedules, referenced asset schedules, and flat-order compatibility. Coverage-row PDF navigation extends `app/policies/[id]/source-provenance.tsx`.
- Extraction completion safety: `extraction-worker/src/pdfSourceFallback.ts` catches only LiteParse conversion failure; model/extraction failure never invokes PDF.js or reruns extraction. `convex/lib/extractionPromotion.ts` owns the extractor-independent evidence ledger, deterministic source-coverage map, completion manifest, and gate decision. `policies.promoteCompletedExtractionInternal` is the sole final-stage writer, validates the current run/lease plus the persisted source bundle and matching v2 section results, and sets `final` internally; generic extraction updates reject `final`. Shadow mode records violations, enforce mode blocks them, and degraded sections always block. `actions/extractionIntegrityAudit` separates legacy-unverified and post-cutover rows and queues only ordinary full re-extraction, including targeted Zurich review.
- Extraction protocol v2: `EXTRACTION_WORKER_PROTOCOL_VERSION=source-tree-v2` enables resumable core, coverage, and optional cleanup sections. Every nonempty span maps to core, coverage, both, or catch-all, party-changing endorsements enter both, and source bundles/section results are keyed by run, source fingerprint, and extractor version. Convex dual-accepts v1/v2; do not require v2 until `compareExtractionProtocolCorpus` reports zero critical-fact and coverage-recall regressions for a representative corpus and the exact section-capable cl-sdk is deployed.
- PDF-work admission: `extraction-worker/src/pdfWorkAdmission.ts` owns the shared full/preview/HTTP admission gate. Acquire it before job claim or HTTP body decoding and hold it for the complete PDF-bearing operation, so queued entries retain metadata rather than file bytes. Defaults allow 12 live PDFs with at most 8 full jobs, preserving capacity for preview/HTTP work; the serialized LiteParse queue has its own 12-document defensive cap. Abort queued admission during shutdown or client disconnect.
- Queue and operator backfill invariants: LiteParse tracks preview and full wait debt independently, so a forced turn for one cannot reset the other's starvation bound. ACORD taxonomy dry runs persist bounded policy pages in `acordTaxonomyDryRunPages`; write runs persist control state and idempotent page results in `acordTaxonomyWriteRuns`, `acordTaxonomyWritePages`, and `acordTaxonomyWritePolicyResults`. Both page each policy's source evidence outside mutation transactions and apply only fingerprint-current decisions. A write mutation commits its policy patch and report ledger entry atomically; retries reuse that result, and aggregate reports read the per-policy ledger so partial-page failures cannot lose committed counts or samples. Write continuations use bounded retry, both modes aggregate through `actions/backfillAcordTaxonomy:report`, and `actions/backfillAcordTaxonomy:resume` reschedules the durable cursor after interruption.
- Public iMessage app cards are default-off behind `imessage_app_cards`. When enabled, links are serialized by `convex/appCardLinks.ts` and rendered by `app/share/imessage/[token]`; they must preserve `policyTermType` so continuous policies render as `Until Cancelled` on every public card surface. Keep the flag check at the link-minting boundary so disabled organizations neither send cards nor create unused public URLs.
- Cross-channel email-draft review links are owned by `convex/emailDraftReviewLinks.ts`, `convex/actions/emailDraftReview.ts`, `app/share/email/[token]`, and the token/hash primitive in `convex/lib/magicLinkTokens.ts`. iMessage draft cards are standard and are not gated by the policy-card beta flag; Slack uses the existing rich/classic card renderers; direct authenticated agent-email replies may include the same review URL. Store only token hashes, bind Send to the exact draft fingerprint, actor, thread confirmation, and expiry, atomically claim provider delivery, revoke replaced per-channel links, and fail closed for edited, stale, cancelled, sent, or expired drafts. Do not persist plaintext bearer URLs in thread artifacts; sweep expired rows and delete thread-bound links during personal iMessage history deletion.
- Producer nomenclature: use “Producer” in UI, prompts, retrieval output, and current documentation, and use `producer` as the canonical operational party role. Source or legacy `broker` and `agent` party roles and `broker*` compatibility fields remain readable but must not surface as split terminology.
- Stored declaration-fact replacement is owned by `convex/declarationFacts.ts`; `convex/backfillDeclarationFacts.ts` provides bounded verification batches, while `migrations:runDeclarationFactsBackfill` serially rebuilds final-policy facts and then syncs each organization profile once through `@convex-dev/migrations`. These paths never schedule model extraction, remain idempotent, and resync only the insured/client profile fact allowlist.
- Policies and broker follow-ups: `convex/policies.ts`, `convex/lib/linesOfBusiness.ts`, `convex/lib/policyProductIdentity.ts`, `convex/lib/policyVersioning.ts`, `convex/lib/policyLookup.ts`, and `convex/lib/policyTypes.ts`. Keep policy classification in three distinct layers: `linesOfBusiness` emits only the 107 current ACORD `LOBCd` values supplied by `@claritylabs/cl-sdk/policy-taxonomy`; coverage rows use an exact ACORD `CoverageCd` only when code or description resolution is unambiguous; `productIdentity` stores source-backed carrier product/plan name and available company product/subproduct codes. Never substitute a filename or policy number for product identity, never infer a LOB from an unvalidated legacy `programName`, leave ambiguous LOBs as `UN`, and treat `programName` as compatibility projection. Stored-evidence repair belongs in `convex/lib/acordTaxonomyBackfill.ts` and the dry-run-first `convex/actions/backfillAcordTaxonomy.ts` action, which pages per-policy evidence outside mutations and applies one compact fingerprint-guarded decision. Archive/restore uses the legacy `deletedAt` storage field while every active operational query excludes archived parents. Policy-update requests use the generic broker-email path instead of policy-change case helpers.
- Final product identity is replacement-authoritative: final extraction clears both `productIdentity` and `programName` when source evidence has no product identity; codes-only identity retains source-backed company product/subproduct codes while clearing any stale product name.
- Direct operator client setup uses `accessType: "operator"` from `convex/lib/access.ts` only when a domain caller explicitly opts an active operator into the selected organization or policy target outside an active matching impersonation. It is not a membership or a general organization-access grant. The operator client page exposes Policies and Compliance as peer routes, with Certificates as a URL-addressable tab inside Compliance; the legacy operator-client Certificates route redirects to that tab. `/operator/clients/[clientOrgId]/policies` reuses the managed-client policy list, but the full policy workspace always uses a dedicated dense operator layout: Details is the default, client overview/party cards are not reused, structured records and source evidence open in the right sidebar, and policy-scoped run diagnostics live on a separate Extraction history tab. A labeled Re-extract action opens a concise right-side drawer for runtime status, full extraction, coverage recovery, supplementary facts, search-index rebuilding, and active-run controls. The global `/operator/extractions` UI and navigation are removed; trace queries remain policy-scoped. Operator Certificates and Compliance use operator-specific dense renderers without operator filter bars while sharing their domain queries, mutations, and detail drawers. Operator uploads retain `uploadedBySide: "operator"`; operator policy and extraction mutations write both policy audit entries and operator `setup_write` audits, while operator compliance writes record domain-specific `setup_write` events. The client sidebar owns a bottom impersonation button across all client routes so client top bars remain scoped to page actions. Active impersonation preserves the operator layouts but keeps direct setup writes read-only until explicitly stopped.
- Certificates: `components/certificates/certificates-workspace.tsx` owns the reusable organization-level certificate workspace and shell handoff; `components/certificates/certificate-workspace.tsx` owns the shared certificate table and detail drawer used by global, operator-client, and policy-scoped surfaces. Policy-scoped tables omit only the redundant Policy column, and drawer archive/restore actions call `convex/certificateLifecycle.ts`. `convex/certificates.ts`, `convex/certificateLifecycle.ts`, `convex/lib/workflows/certificateRequest.ts`, `convex/lib/certificateRequestGate.ts`, `convex/lib/certificateHolderPopulation.ts`, `convex/lib/certificateIdentity.ts`, `convex/lib/certificateHolderResolution.ts`, `convex/lib/certificateDescription.ts`, and `convex/lib/coiGenerator.ts`. `CertificateData.propertyInformation` carries source-backed commercial-property declarations and structured location details, while `CertificateData.coveredAssetSchedules` carries extracted vehicle, property, location, and other scheduled-item declarations into certificate forms. `coiGenerator.ts` owns the dedicated ACORD 24 matrix, the purpose-specific ACORD 27-31 table renderers, and structured coverage/location/covered-asset continuation schedules. Premises and covered-subject-matter fields use declaration-backed locations and scheduled assets, never a generic policy summary. Property tables render only populated, source-backed sections and rows; omit empty template slots and unchecked placeholder options. Scheduled property values are not coverage limits: never copy, sum, or infer them into limit or deductible cells, and leave unavailable cells within otherwise populated rows blank.
- Compliance and Connect: `components/compliance-page.tsx` owns the reusable compliance workspace and accepts an explicit organization context plus shell handoff for operator-client reuse. `convex/actions/complianceRequirements.ts`, `convex/actions/ownComplianceMonitor.ts`, `convex/compliance.ts`, `convex/lib/complianceTypes.ts`, `convex/lib/complianceCheck.ts`, `convex/lib/complianceAgent.ts`, `convex/lib/vendorComplianceTools.ts`, `convex/connectedOrgs.ts`, and the `connect_features` flag own the backend path. `normalizeRequirementLineOfBusiness` in `convex/lib/complianceTypes.ts` is the canonical normalization boundary for requirement imports, migrations, and policy-LOB comparisons; it maps the requirement-domain compatibility code `CRIME` to `CRIM` even though the broader policy taxonomy remains read-compatible with both. Imported sources persist only after successful extraction; successful exact-duplicate imports retain a zero-row audit source, and typed semantic identity owns deduplication. Across web, Slack, email, and iMessage, an explicit requirement or compliance request with a new agreement, contract, lease, insurance schedule, or requirements packet forces `import_requirement_attachments` before analysis unless the user explicitly forbids persistence. The executor resolves only server-provided current-message files, uses the canonical `complianceRequirements.importRequirementsInternal` source-and-extraction path, and requires a fresh `lookup_compliance_requirements`; never compare a new requirement attachment to policies ad hoc. Generic display limits never prove typed per-claim, per-occurrence, or aggregate terms. Missing structured deductible, form, retroactive-date, provision, or endorsement evidence remains unverified; explicit mismatches remain not met. Saved agent reviews are authoritative until the requirement changes, and certificate preflight carries their rationale while blocking generation when no selected requirement is met or expiring soon. Own-insurance monitoring is org-level, final-policy-only, and independent of mailbox connections.
- Requirement source contacts: `convex/actions/complianceRequirements.ts` extracts every explicit certificate holder or recipient in the same structured pass as coverage requirements, `certificateHolders` owns normalized company/contact records, and `requirementSourceDocuments.certificateHolderIds` preserves source order. Keep `certificateHolderId` as the primary compatibility pointer until certificate generation supports selecting among multiple holders; never discard the remaining extracted contacts.
- Agent/chat controls: `convex/lib/agentPrompts.ts`, `convex/lib/agentScope.ts`, `convex/lib/agentPolicyFocus.ts`, `convex/lib/chatTools.ts`, `convex/lib/agentToolExecutors.ts`, `convex/lib/agentToolAudit.ts`, `convex/lib/agentPolicyPresentation.ts`, `convex/lib/mapboxAddress.ts`, `convex/lib/agentMessageHistory.ts`, `convex/lib/taskControlIntent.ts`, `convex/lib/webChatDeterministicControls.ts`, `convex/lib/textChannelControls.ts`, and `lib/confidence.ts`. Authenticated web/Slack, email, iMessage, and MCP turns load policy data/evidence, company memory, requirements, and vendor compliance only through tools. `lookup_policy` accepts search, up to five exact IDs, and an expiration window; `lookup_company_context` returns company facts only. Current explicit policy targets win; otherwise at most five readable/non-archived IDs from the immediately previous completed agent response are carried as fact-free routing hints and must be refreshed through policy tools. Policy retrieval is evidence only and never emits cards. On supported web, Slack, and iMessage responses, `present_policy_card` may select only a policy successfully resolved by another current-turn tool, defaults to one card, permits a bounded multi-card response only for an explicit multi-policy/link request, and suppresses recently presented policies unless the user asks again; generated certificates retain their separate card path. `lookup_address` permanently geocodes user-provided postal addresses through Mapbox before persisted agent workflows such as `generate_coi`; ambiguous results require user confirmation and must not replace source-backed policy-party facts. Public thread queries and `AgentActivity` expose auditable tool activity but never provider reasoning or reasoning steps; raw model thoughts can repeat hidden instructions and must not render in customer chat. Confidence markers may wrap adjacent inline Markdown nodes; the shared renderer preserves nested emphasis/strong nodes and normalizes the common malformed `[[g]:` opener before rendering, summarizing, copying, or plain-text conversion.
- Multimodal channel routing: tenant web chat, tenant iMessage, and the operator agent keep text-only turns on `chat` and route image-bearing model input through `chat_vision`, preserving the same authorized tools and side-effect controls. Operator PDF, spreadsheet, document, and text attachments use the same bounded attachment converter without changing the operator's required manual route. Channel ingress owns attachment transport and storage; do not create a second image-analysis agent path, pass current image parts to the text route, or let file contents alter authorization or confirmation policy.
- Email, notifications, connected mailboxes, and iMessage: `convex/lib/resend.ts`, `convex/lib/emailDelivery.ts`, `convex/emailDeliveryAttempts.ts`, `convex/lib/emailTemplate.ts`, `convex/lib/notificationEmailTemplate.ts`, `convex/lib/emailSubagent.ts`, `convex/lib/inboundEmailParser.ts`, `convex/lib/forwardReplyDirection.ts`, `convex/threadActionConfirmations.ts`, `convex/lib/emailWorkflow.ts`, `convex/lib/emailDraftService.ts`, `convex/lib/emailDraftArtifacts.ts`, `convex/lib/emailPayloadFields.ts`, `convex/lib/emailCommandExecutor.ts`, `convex/lib/emailIdentity.ts`, `pendingEmails.sendBlockedReason` for unattended draft send blocks that explicit Send actions satisfy, `convex/lib/notificationTypes.ts`, `convex/lib/notify.ts`, `convex/notificationPreferences.ts`, `convex/connectedEmail.ts`, `convex/actions/connectedEmail.ts`, `convex/actions/connectedEmailScan.ts` (proactive + manual date-range scans), `convex/lib/imapMailbox.ts` (shared IMAP/crypto helpers, node-only), `convex/connectedEmailAutomation.ts`, and `convex/lib/mailboxAutomation.ts` (automation policy, decision sanitation, and supported-attachment classification), plus `components/settings/email-connection-*.tsx`, `components/agent-thread/artifacts/mailbox-task.tsx`, `components/agent-thread/artifacts/mailbox-email-review-sidebar.tsx`, `convex/lib/threadAccess.ts`, `convex/lib/imessage*.ts`, and `imessage-worker/`. Missing recipient, subject, or body fields and COI attachment safety remain hard blockers after confirmation. Web-chat approval phrases may deterministically send only the single draft linked from the immediately preceding response; agent-approved sends update and transition that same thread-and-recipient draft to pending/sent, cancelling older matching duplicates, while draft-update and sent claims require confirming email-tool results so stale artifacts and prose-only sends cannot pass as completed work. `imessage-worker/src/outboundText.ts` owns agent-authored outbound formatting: Spectrum-space delivery wraps source in native `markdown()` content, and low-level Photon chat-GUID delivery renders that source as readable plain text so raw Markdown and Spot confidence markers never leak. `imessage-worker/src/terminalIdentity.ts` owns local Spectrum `/whoami` and `/as` identity selection; keep that test-only behavior out of the Photon transport. `imessage-worker/src/voiceAttachment.ts` owns inbound voice-memo M4A normalization before Convex; `convex/lib/imessageAgentContext.ts` owns the one shared tenant/public/operator preparation handler that bounds audio, selects audience-appropriate transcription routing, and emits labeled text before the separate agent cores run. Keep codec/container repair in the worker and keep operator and tenant authorization, persistence, and tool execution separate. The iMessage worker sends the Spot contact card once for each direct iMessage chat when Convex returns `sendContactCard`, preferring Photon/Spectrum native contact sharing and falling back to a vCard with the Spot number, agent email, website, and notes. External proactive delivery goes through `notify`; mailbox automation stores cursor/outcome metadata but not raw message bodies. Thread-linked review renders one subject pill per email in the agent message; each pill opens only that live message in a dedicated sidebar, whose footer resolves the item as `Not relevant`, policy import, or requirements import. Gate vendor/internal requirement choices with `connect_features`; otherwise show one `Import insurance requirements` action. Personal-mailbox activity and linked-user direct iMessage threads use `visibility: "user_private"` with `createdBy` as owner; groups remain org-visible.
- Customer-visible agent response safety: `convex/lib/agentMessageHistory.ts` keeps tools, workflow outcomes, and attachments in private structured metadata; new history text never receives a trailer. It and `imessage-worker/src/outboundText.ts` remove only an exact legacy end trailer. `convex/lib/confidence.ts` owns compatibility parsing, `convex/lib/transportRenderers.ts` owns token-based email/Slack rendering, and `imessage-worker/src/responseDelivery.ts` owns codepoint-safe response segmentation and one native reply operation per bubble.
- Conversation continuity and privacy: `convex/lib/agentMessageHistory.ts`, `convex/agentHistory.ts`, `convex/actions/compactThreadHistory.ts`, `convex/lib/agentHistoryLoader.ts`, and `convex/lib/slackThreadContext.ts` are the only model-history owners. Every authenticated agent surface declares an exhaustive channel policy; web, Slack, email, and MCP use thread-long continuity, while iMessage uses task-scoped continuity with explicit resets only; inactivity preserves the task and summary. Exact context is at most 24 complete user turns with paired replies and a conservative 32K-token prior-dialogue budget; the current request is never clipped. After the current Slack actor is authorized, channel-thread invocations fetch a bounded exact source-thread snapshot through the authenticated worker and add only root or ambient human messages absent from durable Spot history; current, previously captured, and Spot-authored messages are deduplicated, the context stays hidden from the visible mirror, and retrieval failure does not block the current request. Rolling summaries stay internal, cannot select policy focus, and never contain attachment bodies or authoritative policy claims. Older history and files are retrieved only through the current accessible thread's bounded tools. `convex/imessagePrivacy.ts` owns direct-iMessage history generations, active-run leases, inventory, and resumable erasure; never reuse a pre-deletion generation, target groups or other owners, or delete a file referenced by a surviving business record. `components/profile/imessage-history-privacy.tsx` owns both profile variants, and `imessage-worker/src/inboundNormalization.ts` owns one-logical-turn Photon text-plus-attachment recovery.
- Slack channel primitives: `convex/agentChannels.ts` owns per-client availability, persisted operator-led setup lifecycle, OAuth state, encrypted rotating workspace credentials, connection lifecycle, membership inventory, automatic routing, and audit; `convex/slack.ts` owns actor classification, thread state, revisions, handoffs, App Home DM privacy, and canonical records; `convex/lib/slackThreadTitle.ts` plus `convex/actions/threadTitle.ts` own channel-aware, first-message topic naming; `convex/slackOutbound.ts` and `convex/actions/sendSlack.ts` own the durable outbound ledger; `convex/lib/channelAgentRunner.ts` is the channel-neutral agent entry; `convex/lib/slackSecurity.ts` and `convex/lib/slackPayload.ts` own the native signed Events API boundary; `convex/lib/slackCredentials.ts` owns workspace-bound credential encryption and refresh; `convex/lib/slackConfig.ts` owns global enablement, mock/live mode, and complete host-OAuth readiness; `convex/actions/slackOAuth.ts`, `convex/actions/slackOnboarding.ts`, `app/operator/channels/page.tsx`, `components/settings/agent-channels-section.tsx`, `lib/oauth-tab.ts`, `slack-worker/manifests/`, and the stateless direct-Web-API `slack-worker/` own setup and transport. `lib/oauth-tab.ts` owns the synchronous, opener-isolated new-tab handoff for customer and operator OAuth setup so async authorization-URL creation does not trigger popup blocking. `/operator/channels` owns the one-time Clarity workspace installation and each operator's Slack identity: disabled environments render as `Slack not enabled`, mock mode renders without OAuth controls, and incomplete live deployment configuration renders as unavailable without exposing environment-variable names. Its operator roster shows the signed-in account first, followed by every other Spot operator, and allows only that account's Slack member ID to be changed through the right-side drawer; retained identities on disabled accounts stay visible for conflict diagnosis. Workspace installation, the Clarity-managed Slack Connect support binding, joined-channel membership, and the automatic alerts-and-delivery destination are separate state: `slackSetupStates` owns one resumable setup or repair lifecycle per client, `slackChannelBindings` is support-only, `slackChannelMemberships` is the synchronized joined inventory, and `slackWorkspaceConnections.automaticChannelId` is the optional automatic destination. Reinstall runs preserve the active connection and every channel, destination, and automation choice until same-workspace OAuth succeeds with all required scopes; cancellation or failure invalidates unused OAuth links without disconnecting the working configuration. The worker inventories every visible public channel plus joined private/shared channels; authenticated web setup may join only visible non-shared public channels, while private and Slack Connect channels must be added in Slack. Spot responds to direct mentions in every joined channel and continues active matching threads there; the support channel is additionally captured without a mention and remains the human-handoff destination. Inbound authorization happens before file retrieval, ignored external/bot ledgers retain no content, and canonical deduplication includes logical channel identity. Clarity Labs creates or reuses the support channel independently of client workspace installation, adds active operators whose Slack identities match the host workspace, reports disabled, unlinked, and mismatched operators, then sends the email-based Slack Connect invitation. Persist the host binding before surfacing membership or Connect partial failures so retries reuse it. Per-client channel surfaces own the client workspace, support invitation, membership, availability, and automations. Photon remains iMessage-only. The worker must resolve a Slack Connect sender's native workspace with `users.info` before Convex authorizes the actor; never default a missing sender team to the installation team. New Slack channel conversations use the resolved `#channel` plus a three-to-four-word topic generated from the first message on the `summary` route; generation is compare-and-set so later manual titles win, model failure uses a sanitized deterministic fallback, and direct-message titles remain person-oriented. App Home DMs are stable per DM channel, run without mentions, and mirror into `user_private` web threads only when `users:read.email` links the Slack profile to a current client member; unlinked DMs stay Slack-only. Sidebar thread splitting pins Slack and iMessage conversations ahead of ordinary web/email threads and Slack rows use the Slack mark. Slack-safe notification classification lives in `convex/lib/notificationTypes.ts`, with routing through `convex/lib/notify.ts` and `convex/notificationSlack.ts`. Do not add a second Slack sender or an operator service inbox. Policy delivery is client-owned through `deliveryOwnerOrgId`; `brokerOrgId` and `broker_review` are optional legacy context during widen/backfill/narrow.
- Slack invocation and mirror invariants: for both clients and operators, signed Slack event delivery to an enabled installed app is the channel-presence authority. Channel inventory, privacy, sharing, membership, and support-binding health never authorize an invocation or reply; replies target the exact delivered channel while the workspace connection is healthy. Those fields remain metadata for setup, web-mirror privacy, canonical support routing, and proactive destination selection. Private non-support mirrors fail closed as `user_private` to the first mapped Spot member or remain Slack-only; inbound activity restores archived mirrors; Slack deletions scrub mirrored content, attachments, revisions, and inbound payloads without invoking the agent; Slack-origin web threads are read-only and link back to Slack; mirrored agent messages carry retrying or terminal delivery state; App Home DM text and files stay top-level; and the owner All threads page keeps Slack conversations beyond the pinned eight discoverable.
- Slack lifecycle and health primitives: `convex/slackLifecycle.ts` owns the durable lifecycle ledger, ordered installation/channel transitions, retained previous channel IDs, and provider-failure evidence; `convex/actions/slackReconciliation.ts` owns periodic token-safe installation and channel verification; `convex/lib/slackAvailability.ts` is the shared outbound health gate. Preserve bindings, canonical records, preferences, and delivery evidence across degradation. Reinstall must retain the same workspace, and replacement Slack Connect bindings require an audited operator rebind rather than name matching.
- Slack rich-response primitives: `convex/slackPresentation.ts`, `convex/actions/slackPresentation.ts`, `convex/lib/slackBlocks.ts`, and `convex/lib/slackInteractions.ts` own model-selected reaction-first processing acknowledgement, one completed emoji-free Block Kit answer, policy/certificate/email-draft cards, feedback, tenant-bound actions, and automatic classic Block Kit fallback. The worker adds `eyes` as the immediate default, `choose_slack_reaction` lets the Slack model replace it with one context-appropriate built-in reaction before other tools, and cleanup removes the selected reaction on success or failure. Keep the reaction presentation-only and out of visible completed-work traces; never post or stream progress narration. Rich responses are the default and do not add a feature flag. Never render model reasoning or raw tool input/output; keep every action signature-verified, bound to the exact tenant/team/channel/message, actor-authorized, and idempotently recorded.
- Thread-linked mailbox review passes the automation item identity to the shared IMAP reader. If a persisted folder UID is stale, `convex/lib/imapMailbox.ts` resolves the stored RFC Message-ID through the provider-listed all-mail or archive folder, returns a current transient email reference for follow-up actions, and surfaces an explicit unavailable state when no live copy remains.
- Company wiki: `convex/orgWiki.ts`, `convex/lib/orgWiki.ts`, `convex/lib/orgWikiPolicy.ts`, `convex/lib/orgWikiExtraction.ts`, `convex/companyInformation.ts`, `convex/lib/agentPrompts.ts`, and the on-demand `lookup_company_context` executor in `convex/lib/agentToolExecutors.ts`. The wiki is one markdown document per organization held as ordered sections from a fixed catalog; agents read it whole, with no ranking or relevance cutoff. Settings → Agent → Company wiki gives direct members read access and direct admins section writes; `/operator/clients/:clientOrgId/wiki` gives direct operators the same and becomes read-only while impersonating. Tenant MCP wiki reads revalidate exact-organization membership and writes revalidate direct-admin plus write scope; operator MCP projects the exact-confirmed section write from the registry. The company-information reconciler rewrites and retracts only the bullets it owns (`extractedLines`), leaving conversational and append-only lines alone, and proposes rather than writes over a manual edit; every automated line passes the company-context gate while human section writes are admin-gated only, bullets are deduplicated and sorted for byte-identical reruns, and source removal is deterministic. Policy facts remain in first-class policy/source tables and never come from the wiki.
- Tenant OAuth MCP: the typed `MCP_TOOLS` catalog in `convex/http.ts` is the single source for tenant tool effects and annotations, and dispatch derives its write-scope gate from the same entry. Read-only `ask_spot` and `ask_glass` calls must pass `canWrite: false` through both the shared client executor and mailbox coordinator so nested write/import/save/invite tools are absent, not merely expected to fail later.
- Public APIs and integrations: `convex/lib/apiAuth.ts`, `convex/lib/apiDto.ts`, `convex/lib/apiError.ts`, and the Convex REST/MCP HTTP handlers. MCP authentication is OAuth-only; local clients use `mcp-remote` against the remote endpoint.
- Workers/deploy: `.github/workflows/deploy-convex.yml` owns release readiness for every production commit, `scripts/wait-for-railway-statuses.mjs` owns the exact-commit four-service Railway barrier, and `extraction-worker/`, `imessage-worker/`, `slack-worker/`, `mailbox-scan-worker/`, `config/deployments.json`, Railway health/env docs in `AGENTS.md`, and `npm run check:shared-package-versions` own the runtime contract. Vercel production promotion requires the unique `release-ready-production` GitHub Deployment Check. Railway Git autodeploy plus every service watch path must remain enabled so each commit reports a deploy or no-op status; do not combine this barrier with Railway Wait for CI. The production compatibility audit runs after Convex and Railway are ready; `.github/workflows/agent-safeguards.yml` exposes the same audit only on manual dispatch. Extraction jobs and authenticated conversions run only in Railway's persistent `dev` and `production` environments; retired, ephemeral, or unknown environments are health-only and expose that mode on `/health`.

- Operator channel settings: `/operator/channels` owns the Clarity Slack installation, each operator's Slack identity, and the authenticated operator iMessage contact-number/status display. The iMessage number comes from Convex `OPERATOR_IMESSAGE_CONTACT_PHONE`, never a public browser variable; the page shows the current operator's linked sender phone and directs missing phone setup to `/operator/profile`.
- Slack channel membership controls may join or leave only visible non-shared public channels. Private and Slack Connect membership is managed in Slack. This inventory supports setup, display, and proactive destination selection; it is never an invocation allowlist. The designated client support channel is one joined Slack Connect channel with additional no-mention capture and human-handoff behavior.

Global router freeze control lives on `/operator/routing` through `clRouterOperations.setGlobalFreeze`. Keep the admin secret server-side, require an active operator, include the operator ID in the router's immutable reason, and target the healthy router configured by `CL_ROUTER_URL` without a separate Spot/router environment lock. Normal deployments keep `CL_ROUTER_FROZEN` and `CL_ROUTER_SHADOW` off; the environment freeze remains a panic switch that the UI cannot override.

- Email-address syntax: `convex/lib/emailAddress.ts` owns shared address extraction, validation, and normalization for inbound and outbound mail paths.

- Slack thread activation is thread-scoped for both tenant and internal operator chat: DMs need no mention; a human `@Spot` mention at the root or on a later reply activates that exact thread; later authorized human replies continue without another mention; and unrelated unmentioned top-level messages remain ignored. A rapid follow-up may join the same inbound batch as its activating mention, but authorization must validate the mention sender before treating the batch as active. Spot-authored messages never activate, continue, or enter source-thread context; other bot messages never activate or continue a thread. Tenant handoff/resolve state and archived internal operator threads deactivate mention-free continuation until a new mention resumes the conversation.

Procurement model routing reuses stable top-level routes: `requirement_extraction` for request intake drafting, `extraction` for proposal documents, and `analysis` for proposal-versus-requirement review. Do not add procurement-specific top-level routes or bypass the shared routing helpers.

## Known Cleanup Targets

Requirement-backed certificate generation uses `components/certificates/certificate-generate-panel.tsx` as the shared policy-first, optional-requirements drawer across global, managed-client, operator-client, and policy-scoped surfaces. `convex/lib/certificateRequirementPlan.ts` maps met or expiring-soon requirements to unique final-policy targets while retaining the chosen primary policy and reporting unmet/no-final-policy gaps. `convex/certificates.ts` persists immutable requirement snapshots and a batch ID on versions and holds; requirement revisions change the reuse signature. Shared `generate_coi` and direct MCP/REST calls use this batch path, and web, Slack, iMessage, MCP chat, and email return every generated PDF without claiming gaps are satisfied.

When the user asks for primitive cleanup, start with branding/email shell ownership, raw settings switches outside `SettingsSwitch`, and any settings/nav section duplication before broader refactors.

- Broker profile editors use record-scoped autosave with field-specific `brokerProfiles.upsert` patches; omitted fields survive concurrent edits, office-address patches merge fields, logo changes touch only the logo, and network status changes require an operator. Selected operator broker details are queried independently of list filters.

- Operator thread table rows open the shared conversation renderer in a sidebar; open-conversation and archive/restore actions live in its footer. `SettingsDrawer.contentClassName` supports flex-filling content without viewport-height calculations. Team profile edits autosave field-specific changes and retain failed drafts; role changes remain explicit. Member activation, primary-contact and email-change actions live in the member sidebar footer, and pending invitations open a detail sidebar for cancellation. Broker app navigation skips tenant-thread subscriptions.

- Team last-admin checks and primary-contact fallback count human memberships only. Hidden channel service accounts cannot replace the last human admin or be selected as the insurance contact.

- Customer approvals and email review links remain valid while the exact draft and task remain current; completed email authorizations also revalidate the task epoch. Signed Slack presentation controls remain usable until revoked, including legacy controls unless they were explicitly invalidated by plaintext fallback. Broker packet links default to no expiry and allow an optional operator-chosen lifetime, while existing expirations stay enforced. `convex/actions/operatorChannelDelivery.ts` continues operator Slack/iMessage response delivery after the bounded synchronous request wait, revalidating operator identity and preserving the channel delivery key.
